# version: '3.8'

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: mydb
    volumes:
      - ../../../simcbx-postresql-data:/var/lib/postgresql/data
    ports:
      - '5432:5432'
    restart: always
